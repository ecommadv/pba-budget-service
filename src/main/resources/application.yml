spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/budget
    username: postgres
    password: larlarlar
    driverClassName: org.postgresql.Driver
  data:
    mongodb:
      database: budget
server:
  servlet:
    context-path: "/api"
  port: 8082
auth:
  base_url: http://localhost:8080
  get_user_url: http://localhost:8080/api/user
currency:
  rates:
    url: https://www.bnr.ro/nbrfxrates.xml
    scheduler:
      cron: 0 0 0 * * *